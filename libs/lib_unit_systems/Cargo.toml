[package]
name = "lib_unit_systems"
version = "0.1.0"
authors = ["roal_yr"]
edition = "2018"


[dependencies]

# Depending on macros so not including in `dep`.
serde = { version = "1.0", features = ["derive"]  }
